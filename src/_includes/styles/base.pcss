* {
  box-sizing: border-box;
}

body {
  color: $c-font;
  background: $c-bg;
}

button {
  background: none;
  border: none;
  padding: 0;
}

button, a {
  cursor: pointer;
  
  &:focus {
    outline: none;
    background: $c-focus;
  }
}

picture {
  display: inline-block;
}

section, nav, header, footer, main {
  display: block;
}

svg {
  fill: $c-font;
}